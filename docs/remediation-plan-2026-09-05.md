# 审计修复与成熟发布计划

基线：`a839042`。来源：`docs/audit-report-2026-09-05.md`。执行模式：持续 goal，逐阶段实现、验证、记录。

## 目标与完成定义

完成 A01–A14 的可执行修复，保留课程/SRS/本地数据契约和四季视觉方向。成熟发布基线包括：破坏性操作有预览与取消；失败原因可理解且可恢复；移动端核心操作直接可达；内容及数据契约有验证；关键流程有 CI 保护；性能和兼容性有可复测基线。语言专家全量审校、真机实验、真实学习效果与线上运营不以本次代码修复冒充完成。

## 阶段 1：教学正确与数据保护

- [x] A01 修正韩文字块拆解；对课程中的拆解示例建立通用一致性检查，验证真实示例和故意错误的反例。
- [x] A02 建立独立备份输入 schema，验证已知字段和嵌套结构；合法旧版本经明确归一化处理，错误输入拒绝且不写存储。
- [x] A03 将文件选择、预览、最终替换拆成三步；显示备份日期、课程/卡片/作品计数、缺失数据与录音清理影响，提供导出当前文字数据、取消和最终替换；操作中禁用重复提交。
- [x] A04 为入门试听建立请求状态、超时/失败处理、重试和暂缓入口，暂缓不生成听力证据。
- 验证：内容检查、备份失败/取消/回滚测试、入门事件生命周期和播放失败浏览器场景。

## 阶段 2：视觉、移动端与大列表

- [x] A05 标题块级布局，隔离装饰与布局职责，检查长标题。
- [x] A06 数据菜单保持稳定可访问名称。
- [x] A07 用 44px 主题选择入口解决窄屏空间与可点击范围矛盾。
- [x] A08 词库搜索前置、介绍收起；听读目录折叠，当前材料可直接操作；切换材料管理焦点。
- [x] A09 词库/语法分页或有界显示，筛选不绕过条数限制，切换页保留明确上下文。
- 设计基线：瓷白 #f4f6f5、深蓝 #172235、雾蓝 #a8b8c8、山茶 #ba5665、新叶 #567c64；Noto Serif KR 标题/韩文，系统 sans 正文/操作；左对齐；工具页为“标题 → 搜索/当前任务 → 内容 → 可折叠背景说明”，首页保留季节场景。复核：不另造视觉体系，不把所有页面统一成长 Hero。
- 验证：320/390/768/1024/1280/1440，四主题、键盘菜单、首屏搜索位置、DOM 上限、长标题。

## 阶段 3：状态恢复与文案

- [x] A10 提交区分陈旧卡/已处理/存储失败；双标签更新显示恢复操作，保持防重复计分保护。
- [x] A14 统一入门/前置/可预览/正式进度口径；统一录音与系统语音说明；失败提示描述本次播放，不归咎于系统缺声；筛选中文化。
- 验证：双标签卡片更新、失败重试、未入门课程桥接、语音故障与结果文案。

## 阶段 4：架构、性能与公开页面

- [x] A11 按职责拆分 workspace 中的 React hook、归一化/领域或推荐逻辑，保留兼容入口；把录音/任务证据等状态明确的 UI 从超大课程页面分离；建立类型明确的内容契约和更严格的检查边界。
- [x] A11 检查共享壳层引入全量数据的原因，移除不必要重复订阅或延迟加载非首屏功能；记录生产 JS/资源及 DOM 基线，并建立预算检查。保持数据驱动接口，不机械拆文件。
- [x] A13 为模块和课程提供独立 metadata；集中站点 URL 配置；公开页面 sitemap/robots 与 canonical；提供部署前校验命令，拒绝发布配置使用 localhost。
- 验证：兼容接口单测、严格边界 typecheck、生产构建、metadata HTTP 检查、性能预算。

## 阶段 5：CI、回归与发布记录

- [x] A12 CI 安装 Playwright Chromium，执行生产 smoke，失败上传日志与截图；补充本轮高价值场景和 1024 宽度。
- [x] 更新 README/设计文档中的失真描述与固定测试数；文档说明数据限制、运行/发布步骤和测试范围。
- [x] 完成 `npm run check:all` 和生产依赖审计；修复所有新增失败；复查 git diff，不修改用户无关文件。
- [x] 输出最终修复矩阵、验证结果、前后对比和仍需人工验证的边界。

## 执行记录

### 开始

- goal 已开启；计划顺序优先保护学习数据和首课正确性。
- 审计基线已完成 387 单测、生产 build、HTTP/浏览器 smoke；后续以当前实际结果为准。

### 实现与第一轮完整检查

- 修正 한글 与新增检查发现的 읽 元音错误。备份新增嵌套类型校验、明确替换预览、取消、录音影响提示和重复操作保护；课程增加录音下载。
- 词汇/语法改为每页 12/6 条，查询不绕过分页；介绍折叠，听读当前材料前置，目录默认收起。主题入口与选项采用 44px 目标，数据菜单保留可访问名称。
- 复习提交新增结构化失败原因，保留旧布尔接口；页面增加跨标签更新提示与队列重读。
- 课程客户端从约 1,680 行缩至约 500 行，结果和录音证据独立维护。React Hook 从 workspace 分离，由单个 Provider 共享订阅。完整课程讲解与运行目录分离，生成文件由 `content:check` 校验。
- 第一轮完整 `check` 通过 394 项测试、两种类型检查和生产构建。最大 JS 621,107 字节 / gzip 204,379 字节；预算为 650,000 / 220,000 字节。HTTP 131 条路由与生产依赖审计（0 漏洞）通过。
- 浏览器新增场景仍在运行，完成后补充最终数据；复核期间新增的备份键名反例也将再次执行。

### 最终验收

- 全部计划项完成。最终 `npm run check:all` 退出码 0：395 测试通过、77 个预渲染条目、HTTP 131 条路由、完整浏览器回归通过。生产依赖审计 0 已知漏洞。
- 继续将领域拆为写入（约 1,500 行）、只读计算（约 980 行）、归一化（约 370 行）、Hook（约 190 行）；备份和上述核心领域均纳入 strict，课程运行目录纳入 checkJs 契约。保留原有公共领域接口，Hook 消费者统一使用显式新入口。
- 最终最大 JS 为 624,396 B，gzip 204,595 B，在 650,000 / 220,000 B 预算内。
- 390px 词汇搜索约 592px、当前听读内容标题约 903px，词库代表性 DOM 为 648；四主题入口和六档宽度检查通过。
- 复核发现 smoke 启动器可能吞掉子进程失败退出码，已修正并用故意缺少浏览器模块验证退出码 1。
- [最终修复与验收报告](remediation-results-2026-09-05.md) 已保存，完整通过日志、测量数据和截图存入 `docs/verification-2026-09-05/`。
